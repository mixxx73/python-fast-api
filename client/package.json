{
  "name": "expense-client",
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "test": "npm run build && node --test"
  },
  "devDependencies": {
    "typescript": "^5.4.0"
  }
}
